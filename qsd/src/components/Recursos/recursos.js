import React, { Fragment, Component } from 'react';

export default class Recursos extends Component {
    render(){
        return (
            <Fragment>
                <div>
                    <h1>Recursos HTML:</h1>
                </div>
            </Fragment>
        )
    }
}